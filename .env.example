# Required — set in Vercel/Netlify dashboard (never in frontend code)
RAPIDAPI_KEY=your_rapidapi_key_here
MAX_API_SLOTS=4

# Live download counter — required on Vercel/Netlify for honest global count (free at upstash.com)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Or Vercel KV when linked:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
